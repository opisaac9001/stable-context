# yawl/requirements.txt

# --- Core API & Schema Dependencies ---
fastapi
uvicorn[standard] # Includes pydantic, starlette, and standard uvicorn features
pydantic
python-multipart # For FastAPI File Uploads (used in /upload_document)

# --- Other Core / General Dependencies (add as identified) ---
numpy>=1.20.0         # For numerical operations, used in semantic chunking and by other libs
# sse-starlette # For streaming if needed beyond basic WebSocket/HTTP
# python-dotenv # For managing environment variables
PyYAML        # For config file loading
# watchdog      # For config hot-reloading (optional)

# --- Tokenizers (beyond basic char counting) ---
# sentencepiece
# transformers[tokenizers] # For Hugging Face tokenizers
tiktoken>=0.5.0               # For token counting, often used with embedding/retrieval

# --- Model Runners & Related Libraries ---
# For LlamaCppRunner (GGUF models)
llama-cpp-python>=0.2.20 # For running GGUF models locally
# For LlavaCppRunner (multimodal - requires manual build of llama.cpp with LLaVA support, then pip install llava-cpp-python)
llava-cpp-python>=0.1.0 # Add when LLaVA multimodal support is fully implemented

# For AWQRunner (Transformers-based AWQ)
transformers>=4.30.0       # For loading AWQ models via AutoModelForCausalLM
autoawq>=0.1.6             # Core AutoAWQ library for AWQ models
torch>=2.0.0               # Required by transformers and autoawq
accelerate>=0.20.0         # For device mapping and optimized inference with Hugging Face

# For EXL2Runner
exllamav2>=0.0.11          # High-performance inference for EXL2 models (GPU required, may need specific build steps)
# torch is already listed above as a dependency for AWQRunner (torch>=2.0.0)

# For APIRunner (example for OpenAI or other HTTP APIs)
httpx>=0.25.0      # For making HTTP requests
# openai     # If using the OpenAI Python client directly

# For VLLMRunner
# vllm       # Core VLLM library (for serving, if not just client)
# httpx      # For asynchronous HTTP requests to the VLLM server (if APIRunner's httpx isn't reused/generic enough)

# For LoRA and other Parameter-Efficient Fine-Tuning techniques
# peft # Hugging Face PEFT library (used with Transformers for LoRA, etc.)
# bitsandbytes # Often used with PEFT for quantization during training/loading

# --- Model Downloading ---
huggingface-hub>=0.17.0       # For downloading models from Hugging Face Hub

# --- Retriever (RAG - Phase 4) ---
sentence-transformers>=2.2.0 # For embedding text
chromadb>=0.4.15              # For vector storage and similarity search
rank_bm25>=0.2.2              # For BM25 sparse retrieval in hybrid search
nltk>=3.8.0                   # For sentence tokenization in semantic chunking
# For PDF Processing (in PdfRetriever)
PyMuPDF>=1.23.0               # For PDF text extraction (replaces pypdf2)
langchain-text-splitters>=0.0.1 # For RecursiveCharacterTextSplitter (fallback chunking)
# langchain             # For text splitting and document loading (optional, can use custom)


# --- Tool-Calling (Phase 4) ---
# For MCP Adapters
langchain-mcp-adapters>=0.1.0
# Add other general tool-related dependencies here if any (e.g., for parsing specific tool inputs/outputs if not covered by basic types)

# --- GUI (Phase 5) ---
# PyGObject (if GTK bindings are needed, less likely with Tauri direct webview)

# --- Testing ---
# unittest (built-in)
# pytest (optional, popular alternative)
# pytest-cov (optional, for coverage)

# --- Development Tools ---
# ruff (linter/formatter)
# mypy (type checking)
# pre-commit (for git hooks)
